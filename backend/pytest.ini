[pytest]
DJANGO_SETTINGS_MODULE = config.settings.development
python_files = tests.py test_*.py *_tests.py
markers =
    integration: tests que requieren una instancia real de Odoo (excluidos por defecto con `-m "not integration"`)
addopts = -ra
