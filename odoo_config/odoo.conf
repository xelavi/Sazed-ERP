[options]
; admin_passwd debe ir en texto plano (Odoo no expande env vars en conf).
; El docker-compose tambien usa ODOO_MASTER_PWD del .env raiz, manten ambos en sync.
admin_passwd = 48e6xbmqru572hp9v3gta1lydcjsfw0k
addons_path = /mnt/extra-addons,/usr/lib/python3/dist-packages/odoo/addons
data_dir = /var/lib/odoo

; XML-RPC API
xmlrpc = True
xmlrpc_interface =
xmlrpc_port = 8069

; Logging por stdout (Docker captura)
logfile = False
log_level = info

; Workers (dejar a 0 en dev, ajustar en prod)
workers = 0
max_cron_threads = 1

; Seguridad
list_db = True
proxy_mode = False
